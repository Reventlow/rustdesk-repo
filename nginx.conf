server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # Package managers and humans both browse the tree.
    autoindex on;
    autoindex_exact_size off;

    location / {
        try_files $uri $uri/ =404;
    }
}
